services:
  floci:
    image: floci/floci:latest
    container_name: floci
    ports:
      - "4566:4566"
    volumes:
      - floci-data:/app/data
    restart: unless-stopped

volumes:
  floci-data:
